name: example
description: "Whisper ggml example"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  # Audio recorder
  record: ^6.0.0
  # Get local path
  path_provider: ^2.1.3
  # Whisper model
  whisper_ggml:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/